import Vue from 'vue'
import App from '../components/App.vue'

new Vue({
  el: '#app',
  render: h => h(App)
})
import Vue from 'vue'
import Wrapper from '../components/Wrapper.vue'

new Vue({
  el: '#lending',
  render: h => h(Wrapper)
})
